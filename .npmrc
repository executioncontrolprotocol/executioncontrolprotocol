save-workspace-protocol=rolling
link-workspace-packages=true
# Hoist workspace packages to root node_modules for examples/, root-cwd vitest, and CLI smoke.
public-hoist-pattern[]=@executioncontrolprotocol/*
