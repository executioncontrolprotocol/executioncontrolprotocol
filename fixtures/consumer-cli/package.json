{
  "name": "@executioncontrolprotocol/fixture-consumer-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Isolated CLI consumer fixture (not an npm workspace). Installed via scripts/consumer-cli-smoke.mjs from packed tarballs.",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@executioncontrolprotocol/cli": "*",
    "@executioncontrolprotocol/core": "*",
    "@executioncontrolprotocol/format-toon": "*",
    "@executioncontrolprotocol/node": "*",
    "@executioncontrolprotocol/types": "*",
    "zod": "^3.24.0"
  }
}
