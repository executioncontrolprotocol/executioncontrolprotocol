/** Current ECP manifest version string. @category Common */
export const LATEST_ECP_VERSION = "1.0" as const

/** ECP semantic version literal. @category Common */
export type EcpVersion = `${number}.${number}`
